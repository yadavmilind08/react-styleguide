const formSectionLayoutType = Object.freeze({
  Horizontal: "horizontal",
  Vertical: "vertical",
});

const formSectionTheme = Object.freeze({
  Default: "default",
  White: "white",
});

const formSectionAlignment = Object.freeze({
  Left: "left",
  Right: "right",
  Center: "center",
});

export {};

export {
  formSectionLayoutType as FormSectionLayoutType,
  formSectionTheme as FormSectionTheme,
  formSectionAlignment as FormSectionAlignment,
};
